# Session hand-off log

Every agent session that changes this repo appends an entry **at the top** (newest
first) before finishing. The next session may be a different agent/model with no
shared context — and possibly a small context window — so this file stays short:

- **It holds the five most recent entries.** After appending yours, if there are
  more than five, move the oldest to the top of `.agents/handoff/YYYY-MM.md` (the
  month in the entry's date), verbatim, in the same commit. Never edit or
  summarise an entry on the way out.
- **Entries are ≤ ~60 lines and carry state:** what was done, what was decided,
  what is half-finished or broken, what comes next. A *lesson* — the trap a test
  fell into, what a review round found and why it was missed — goes under its own
  heading in `.agents/lessons.md`; the entry links it in one line. *Procedure*
  that changed — how to run a check, which reviewer for what — is edited into
  `.agents/testing-and-review.md`, not narrated here.
- **The newest entry is self-sufficient about live state.** If something from an
  older entry is still true and still matters — an in-flight decision, known
  breakage, a sequencing constraint — restate it or link it. Older entries in this
  file are history and rotation will drop them; nothing live may depend on one.

Older entries live in `.agents/handoff/YYYY-MM.md`, verbatim, newest first. Do not
read an archive file whole. To find something:

```bash
grep -n '^## ' .agents/handoff/*.md      # every archived title, with its date
grep -rn '#123' .agents/handoff/         # every mention of an issue or PR number
```

then read the entry that matched.

Template:

```markdown
## YYYY-MM-DD — <agent> — <short title>
- **Done:**
- **Decisions:**
- **State:** (tests? migrations? anything half-finished?)
- **Next:**
```

---

## 2026-09-19 — Claude Code (Fable 5.1) — **PR #274: Codex round 1 was GO with four P3s; all four reproduced at `2844cca`, fixed and answered at `27f7730`** — the action bar's labels, Data management's focus across a turn (and a commit-order race under it), every state of the picker, the stepper's stability between the samples; a fifth found from the round's own mutants; **a round 2 on the delta is briefed; the merge is the owner's word**

- **Done:** (1) the dialog bars' buttons keep **4 px of side padding, in px** (`Modal.tsx`, both bars): `Button`'s rem padding was 60 of Cancel's 71 px column at a 40 px font on 320 px, and "Cancel" was six lines of a letter; `min-h-12!` — `Button`'s `touch:min-h-10` is emitted later and won otherwise ("is a bar button" caught it). (2) The controls Data management does not render below 768 px sit in one box naming `settings-sections` (the bar's way back) as their stand-in — **and `useFocusAcrossShells` looks again on the next frame** (`lib/focusKey.ts`): every `useShell()` caller subscribes its own media-query lists and commits on its own, so `Layout`'s effect could run *before* the section removed the focused control. (3) A phone's dialog frames are `break-words` (a portal inherits nothing from `main`), and every state of the picker gives way: a result's name keeps 8rem beside what is on hand or that takes the next line, the offer to create breaks, *back to search* wraps with its padding in px. (4) `lists.spec.ts` holds the ten-digit stepper for **sixty frames at 420–540 px**; the true M25 (one ref, on the drawn count) dies. (5, mine) the order form's checkbox column was `flex-col` **and** `flex-wrap` — a multi-line container, so "stretch" stretched rows to the longest word, 20 px past the form at 40 px, inside the sheet's padding where nothing looked: `max-md:flex-nowrap`, the labels' words in spans. Tests: `expectLabelReads`, `expectItsRoom`, `said past its own box` in `expectDialogFits` (one named exception: the head's Close); the turn test focuses Apply, a template ×16, the mode, Preview. Docs: design §13.7, two lessons ("The effect ran before the thing it answers for", "The spill landed in the padding").
- **Found:** my **first** remedy for (2) answered the removal from the removed control's `focusout`: 24 green Chromium turns, then red in one WebKit run of four — **WebKit does not reliably fire `focusout` for a node taken out of the page**. The second remedy is one `requestAnimationFrame` in the existing effect; the listeners are as at `2844cca`. The race's rate moves with the machine: with the recheck out, the test was green twice and then red six times running. Codex's corrections, all taken: the negative control is 12 failed executions across 10 titles (not "9 red"); call 2's rationale was wrong (`display: none` also leaves the a11y tree); **"no default-font phone layout moved" was false** — a kit's series and a low-stock badge now take a second line on 320/390 px instead of being cut (kept; call 11 in the PR body, for the owner to see); the stepper's focus-after-press is #268's, not claimed.
- **Decisions:** the owner's (2026-09-19): "go ahead" — commit, push, the PR body, the reply, this entry. Mine: all four fixed rather than filed (1, 2 and 4 are the PR's own; 3 is the family it sweeps); a round 2 recommended because `useFocusAcrossShells` changed and my first attempt at it was wrong — the brief is written, whether to run it is the owner's. **Open:** round 2; the merge.
- **State:** `feat/260-phone-pages` = **`27f7730`** (two commits on `35d08b1`), pushed; PR #274's body rewritten (round-1 block, 12-mutant table, corrected negative control, calls 2 and 11, coverage rows); the reply posted (issuecomment-5741356230). At `27f7730`: full e2e from a database migrated from empty, `--workers=1`, **175 passed, 55 skipped**, 0 rows left; unit 630; build clean; lint at the one old warning; **12 round-1 mutants, 12 killed** (R4, R6b, R8 on a second pass; R3 is the race); WebKit `phone` + `tablet`, six specs: 74 passed / 36 skipped; 88 WebKit and 44 Chromium turns all on *All settings*; parity from 768 px up for New order, the picker's results and new-item states, Edit order and Data management at 1440/1280/820 × two themes: identical or `main`'s own ≤ 14 pixels at delta ≤ 2. **One unattributed red, said in the reply:** `lists.spec.ts` › *the keyboard keeps its place on a row* failed once under WebKit `tablet` in a 110-test run while the *first* remedy was in the tree; not reproduced in 13 further runs with either version. Not checked by anyone: as the previous entry lists. Throwaway database dropped, worktrees removed, no `zz-*` files, ports free, previews stopped; the iPad mini simulator still booted. Drafts in the gitignored `.dev/260/` (`review-reply-1.md`, `commit-msg-r1.txt`, `review-brief-2.md`, `mutants-r1.txt`).
- **Next:** if the owner wants it, **Codex's round 2 on the delta** — one line pointing at `.dev/260/review-brief-2.md`; findings continue the numbering (5, 6, …); by the #266 rule a GO with nothing above a P3 remedy ends the cycle. Then the merge on the owner's word (`gh pr merge 274 --squash --match-head-commit`, after CI; `Closes #269, closes #270, closes #271, closes #273` — not #260). Then **PR B** for #260: the phone screenshot set (`SCREENSHOTS_OUT`) and the README's one phone capture, the docs-site page, design §13.7 and the roadmap rows marked built (design.md's row 12 is stale), the release — the version, tag and notes are the owner's. Still Jamie's: the real-device pass, the Gunpla skill refresh. Open: #260, #268, #28, #238, #247, #223–#227, #230, #249 — and #269–#271, #273 until #274 merges.

## 2026-09-19 — Claude Code (Fable 5.1) — **#260's first PR built and open: PR #274 at `2844cca`** (M6.6 4/4, the pages and the siblings: Settings, Home and the sign-in screens on a phone; the phone shell holds under a 40 px browser font on a 320 px phone — closes #269, #270, #271, #273); two owner's calls made on an iPad mini in the simulator; **Codex briefed; the screenshots, docs and release are a second PR**

- **Done:** the owner chose the split (2026-09-19): **PR A** the pages and the four siblings, **PR B** the phone screenshots, the docs-site page, §13.7 marked built, the roadmap rows and the release — so #274 says "Part of #260" and does not close it. Settings is one `<nav>` in two shapes (`SettingsPage.tsx`, `SettingsIndex`): a pane from 768 px — it broke at `sm`, 640, before — and on a phone `/settings` is the section list, a section opens from it, the bar's chevron *All settings* (`PageHeader`'s `back`, a slot) is the way up; focusKey stand-ins carry the keyboard across a turn. Data management below 768 px *renders* its export card and one sentence (`data.phoneNote`); the import's state sits above the cards and survives a turn. Home: a completed row is a two-line grid with the pencil beside both; *view all* 44 px; a mail card's words end at its pencil. The font-size family, in the primitives: the phone bar wraps (**the tab bar was never too wide — it is fixed to a layout viewport the overflowing action had widened**, which is what #269 measured); `main` is `break-words`, `Button` and `Chip` labels `max-md:wrap-anywhere`; `CardMeta` wraps (`CARD_WORDS`); the stepper's measured second arrangement (#271); the picker's chip breaks, its row wraps by `stack-3:` (#273); the token card, the pager, About's row, the sign-in wordmark `min(1.5rem, 9vw)`. Tests: `e2e/pages.spec.ts` (new, 10 × 3 projects), `e2e/phone.spec.ts` (new: the happy path by thumb, `phone` only, signs in with the e2e password), the font-size tests of `lists.spec.ts` and `dialogs.spec.ts` extended (facts, a ten-digit count, the pager, a 92-character unbroken name), `shell.spec.ts` knows `/settings`. Docs: design §13.7 "Built — #260", `AGENTS.md` layout notes, README row, `testing-and-review.md`, two lessons.
- **Found:** (1) my first stepper measured the *drawn* count: stacked, a ten-digit count wraps narrower than the line needed, fitted it, and did not again — every frame; the ten-digit tool the test seeded found it; fixed with a hidden ruler (`lessons.md` → "The answer changed what was measured"). (2) The pager under a large font, found only when `pages.spec.ts`'s own seed gave Kits a second page ("The state that was not on the page"). (3) 7 of 31 mutants survived the first pass: four were gaps in what the tests asked (fitting is not reading; inside the screen is not inside the card; a 46-character name was narrower than the desktop's row → 92), two were redundant code and came out, one was my mutant. (4) WebKit writes en-AU dates unpadded ("19/9/2026") — a Chromium-shaped regex timed out there (`A_DATE`).
- **Decisions:** the owner's, all 2026-09-19: the two-PR split; on the iPad mini (744 px) **the completed row stacks by its strip's box, under 26rem, not by the shell** (two lines at 712 px left the rows empty; 26 and not 28 so a 1024 px tablet's 436 px two-up strip keeps what it had), and **the mail stays two abreast there** (Home lays out by its box); commit, push, open the PR; this entry. Mine are calls 1–10 in the PR body; the ones to look at: the bar reads "‹ Settings" with the `h1`/`h2` unforked (1), the cards unrendered not hidden (2), `wrap-anywhere` confined to the phone shell (5), **the bound claimed is 40 px on 320 px and 48 px is not** (7).
- **State:** `feat/260-phone-pages` = **`2844cca`** (one commit on `main` `35d08b1`), pushed; **PR #274** open against `main`, body in the review shape; CI had 2 passing, 3 pending, 0 failing when this was written. At `2844cca`: full e2e from a database migrated from empty, `--workers=1`, **175 passed, 55 skipped**, 0 rows left; unit 630; build clean; lint at the one old warning; negative control on `main`'s `src/` **9 red by name** (green there by design: the wide shapes, the download, `phone.spec.ts`, the auth screens at the default font, #273 in `app`); **31 mutants, 31 killed** (7 on a second pass); WebKit `phone` + `tablet`: pages, phone, dialogs, dialog-keyboard, shell, lists green; parity from 768 px up, 84 captures + the chosen-item row + Home at four widths against `main` on one seeded database: identical or inside `main`'s own noise. **iOS Simulator (iOS 27):** iPhone 17 — Settings, a section with the keyboard up, Data management, Home; iPad mini (A17 Pro) — the sign-in screen with a real keyboard, Settings portrait → landscape with a draft and the keyboard kept, Data management both ways, Home at 744 px. The owner signs in and **rotates the simulator himself**: it runs headless in the app's panel, `simctl` has no rotate, an agent cannot turn it. Both simulators' Safari now hold a dev-database session; **the desktop app's browser pane no longer does** (it showed the sign-in screen — signed-in checks go through Playwright on the throwaway database). Not checked by anyone: the sign-in keyboard on an iPhone-sized screen, a real device, the standalone window, a screen reader, RTL, page zoom, 48 px fonts, Firefox, the packaged stack. Observed and left: on iOS the tab bar rides above the keyboard while a page field has focus (#257's). Throwaway database dropped, worktrees removed, no `zz-*` files, ports free, previews stopped; the iPad mini simulator left booted. Drafts in the gitignored `.dev/260/` (`review-brief.md`, `pr-body.md`, `commit-msg.txt`, the mutant tables).
- **Next:** the **Codex review of PR #274** — the old way, through the PR thread (a browser leg): the owner pastes one line pointing at `.dev/260/review-brief.md`. Answer it per `.agents/testing-and-review.md` → "Responding to a review"; by the rule agreed on #266 a GO with nothing above a P3 remedy ends the cycle; the merge is the owner's word (`gh pr merge 274 --squash --match-head-commit`, after CI). Then **PR B** for #260: `e2e/screenshots.spec.ts` gains a phone set in both themes (`SCREENSHOTS_OUT` for the docs site) and the README one phone capture; the docs-site page on using plamotrack from a phone (DeusMaximus/plamotrack-docs); design §13.7 and the roadmap rows marked built (design.md's roadmap row 12 still says "the first two … built"); the version, tag and notes are the owner's. Still Jamie's: the real-device pass, the Gunpla skill refresh at upgrade time. Open: #260, #268, #28, #238, #247, #223–#227, #230, #249 — and #269–#271, #273 until #274 merges.

## 2026-09-18 — Claude Code (Fable 5.1) — **#259 MERGED as `69de3e3`** (PR #272, squash: M6.6 3/4 — dialogs as full-screen sheets on a phone, order lines that stack, rows that fold by their box, the focus trap holding in WebKit); **#259 and #267 closed**; two Codex rounds, both GO, then an iOS Simulator pass with three fixes; sibling **#273** filed; next is **#260**

- **Done:** the owner's word was "merge it once CI is green" — Backend, Frontend, Integration and CodeRabbit all green at `695808d`, then `gh pr merge 272 --squash --match-head-commit` (no auto-merge: the repo has no required checks, so it could have landed early), the subject the PR's title, the body from the four commits. `main`'s code is the reviewed head's, line for line (checked with `git diff 695808d main`).
- **What the PR was, in one place:** `Modal` draws three frames from one tree by CSS; forms *describe* their actions and Delete (`DialogAction`) and the frame draws them; Delete twins keyed `dialog-delete`; the order form's head one six-column grid and its line a card in the desktop's DOM order; `stack-3:`/`stack-2:` container variants; the trap hand-drives Tab across what Safari skips, a date input keeps its parts and its landing is reconciled once (*a pending correction is consumed by its own departure*); the picker keeps the input's focus through a mousedown; on iOS the panel follows the visual viewport, the focused field is scrolled back into the sheet, a date input keeps to its cell. Round 1 (GO, 2 × P3): the applied-upgrade row under a large font; WebKit passing *Add line* over. Round 2 (GO, 3 × P3): two were round 1's own regressions and were fixed (the reconcile's lifetime; *Withdraw…*'s word), the third predates the PR → **#273**. Then the simulator found three things no suite can see.
- **Decisions:** all the owner's, 2026-09-18: build it; fix rather than file in both rounds where the defect was the PR's own; no round 3; the simulator check; the merge. Mine are calls 1–12 in the PR body; the two worth remembering — the line keeps the desktop's DOM order (the issue drew the name first), and the desktop's line row keeps an accidental 4 px for parity.
- **State:** `main` = **`69de3e3`** + this entry; `feat/259-dialog-sheets` at `695808d` still on origin (deleting merged branches is on the owner's word, as before). Evidence at `695808d` is in the PR body's record and the thread: full e2e from empty 156/37/0, unit 630, WebKit local green, 18 + 5 + 2 mutants killed, parity inside the noise but the declared sub-pixel Apply button. **Not checked by anyone:** a real iPhone, an iPad, the standalone window, a screen reader, RTL, page zoom, the packaged stack. Throwaway databases and worktrees removed, no `zz-*` files, ports free, previews stopped; the iPhone 17 simulator left booted (its Safari holds the owner's dev-database session — signed-in checks there need no password).
- **Next:** **#260** — Home, Settings, sign-in on a phone, the full phone e2e, screenshots and the release — with #269, #270, #271 and #273. Scoped read-only on 2026-09-18: Settings is the real work (it breaks at `sm`, 640 px, not the shell's 768, and has no phone section-list screen — `/settings` redirects to General); Home already lays out by container query; the four siblings are one family (rem-sized or free-text content beside a control that cannot give way) in `ui.tsx`, `Layout.tsx` and the picker; suggested split, the owner's call: the pages and siblings first, then the e2e sweep, screenshots, docs and release. **Run the iOS Simulator early on #260**, and the phone specs under WebKit (`testing-and-review.md`). Still Jamie's: the real-device pass, the Gunpla skill refresh at upgrade time. Open: #260, #268–#271, #273, #28, #238, #247, #223–#227, #230, #249.

## 2026-09-18 — Claude Code (Fable 5.1) — **PR #272 at `695808d`: the iOS Simulator pass that call 8 was waiting for, and three fixes from it** — the tapped field hidden under the raised bar, the page showing through, a date input 30 px out of its cell; both Codex rounds were GO and the cycle is over; **the merge is the owner's word**

- **Done:** the simulator's Safari (iPhone 17, iOS 27) still held the owner's signed-in session, so the keyboard check ran without a credential being typed — the branch served by the previews on the dev database, driven by taps and screenshots. Found, fixed and re-measured there: (1) the action bar did rise above the keyboard, but the **tapped field was hidden** under it — iOS scrolls a field into view against the viewport as it was before the sheet shrank; `useFollowVisualViewport` (`Modal.tsx`) now scrolls the focused field back into the sheet's scroller on a viewport change and on `focusin` while shrunk, and the scroller has `scroll-py-4`; (2) the **page showed through** between the bar and the keyboard — the *panel* follows the visual viewport now (`margin-top`/`height` through the CSSOM), not the overlay, and the phone's overlay is `max-md:bg-surface`; the bottom sheet does not follow at all (no field in it raises a keyboard); (3) **iOS draws a date input at an intrinsic width and lets it out of its grid cell** — `Input` (`ui.tsx`) gives `type="date"` `max-md:min-w-0 max-md:appearance-none`; the system picker still opens (checked). Docs: design §13.7's keyboard bullet rewritten as measured; `testing-and-review.md` → "The iOS Simulator is the only place an on-screen keyboard exists".
- **Found:** no suite can assert any of the three — the evidence is the simulator's screenshots plus unchanged green suites, and the PR says so. Left alone: a sliver of the page behind the keyboard's translucent top edge (Safari's). iOS's keyboard "next field" arrow is greyed on the order form's Tracking URL (iOS's call).
- **Decisions:** the owner's (2026-09-18): run the simulator check, then "go ahead" — full suite, parity, commit, push, PR body, a note on the thread, hand-off. No round 3 asked for: two files, phone frame only. **Open:** the merge (`Closes #259, closes #267`); a real iPhone, an iPad, the standalone window (the owner's device pass — the simulator was one size).
- **State:** `feat/259-dialog-sheets` = **`695808d`** (four commits on `main`), pushed; PR #272's body updated (call 8 rewritten, a *simulator* row in the record); the note posted (issuecomment-5729458104). At `695808d`: full e2e from a database migrated from empty, `--workers=1`, **156 passed, 37 skipped**, 0 rows left; unit 630; build clean; lint at the one old warning; Chromium `phone`/`tablet`/`app` dialogs + keyboard + shell + lists 91 passed / 36 skipped; WebKit `phone` keyboard + dialogs 13 passed / 2 skipped; desktop parity of New order, Edit order and Edit kit at 1440/1280/1024/820 × two themes against `cdebc8e`: 21 of 24 identical, three at 2–4 px and a delta of 1 (the head against itself: the same 4). CI on `695808d` had just started. Throwaway database dropped, worktrees removed, no `zz-*` files, ports free, previews stopped; the iPhone 17 simulator left booted with New order open in Safari.
- **Next:** the merge on the owner's word once CI on `695808d` is green — `gh pr merge 272 --squash`, the subject from the PR title, the body from the four commit messages; the branch stays on origin until the owner says. Then **#260** (scoped 2026-09-18: Settings is the real work — it breaks at `sm` not the shell's 768 and has no phone section-list screen; Home is mostly there; #269/#270/#271/#273 are one family; suggested split, the owner's call: pages and siblings first, then the e2e sweep, screenshots, docs and release). **For #260: run the simulator early** — it found three defects here after two GO reviews. Still Jamie's: the real-device pass, the Gunpla skill refresh. Open: #259–#260, #267 (with this PR), #268–#271, #273, #28, #238, #247, #223–#227, #230, #249.

## 2026-09-18 — Claude Code (Fable 5.1) — **PR #272: Codex round 2 was GO with three non-blocking P3s — the cycle is over by the #266 rule; the two that were round 1's own regressions fixed at `cdebc8e`, the third filed as #273; the merge is the owner's word**

- **Done:** (3, P3) the landing check after a date input (`Modal.tsx`, round 1's fix) removed itself on `setTimeout(…, 0)` alone; under a burst of undelayed native Tabs several were pending at once, each with its own next stop, and they bounced focus between two fields until the page stalled (12/12 for Codex; at `b365c10` my test never returns). The listener now removes itself **before** it sends focus anywhere; the timer only clears one whose Tab stayed inside the field. The invariant is in the code and the test: *a pending correction is consumed by its own departure and governs no later focus change.* (4, P3) *Withdraw…* is 252 px under a 40 px font in a 178 px row — round 1 had removed its word-breaking as untested; back as `stack-2:[overflow-wrap:anywhere]` on the button. (5, P3) a selected catalog item's long unbroken name pushes *Change* to 525 px on a 320 px sheet, identical at `ac00aad` — **filed as #273** (M6.6, with #269–#271). Tests: *a burst of Tabs through a date input settles* (Chromium, three projects); the font-size test **isolates the sheet** (`isolateSheet`: `#root` out of layout while measuring, the screen's width asserted). Docs: design §13.7, one lesson. PR body corrected as Codex asked (the `[274–476]` red with the current seed; the date test's engines; the stale untested row).
- **Found:** why round 1's test missed finding 4 — at 32/40 px the page *behind* the sheet is wider than a phone (#269, #270), a wide page widens the layout viewport (in a mouse context too: `innerWidth` read 381), and the sheet was measured against that. Codex's short-kit seed did not help here (the scale is derived from the grade; the card's chips alone overflow). Codex's candidate also had `max-width: 100%`; with either class alone the button's *box* fits, both single mutants survived, and the cap was removed as redundant. What the checks still do not see: text ink outside a control's box.
- **Decisions:** the owner's (2026-09-18, remote): fix 3 and 4 here rather than file them — they were regressions of this PR's own commit — file 5, no round 3, and **the merge waits for an explicit word**. Mine: a third finding in the trap's segmented-field branch means restructuring it, not patching (said in design §13.7 and the lesson). **Open:** the merge (`Closes #259, closes #267`); the iOS on-screen keyboard on a device (call 8; neither Codex nor this agent can raise one).
- **State:** `feat/259-dialog-sheets` = **`cdebc8e`** (three commits on `main`), pushed; PR #272's body rewritten (round-2 marks, mutants, parity, *r2* rows); the reply posted (issuecomment-5728706950); **#273** filed. Evidence: finding 3 red at `b365c10` as the stall (a 30 s timeout, not a named assertion — said in the body), finding 4 red on "Withdraw… [71–323] of 320" / "outside its box [40–280]" / "323 in 320"; full e2e from a database migrated from empty, `--workers=1`, **156 passed, 37 skipped**, 0 rows left; unit 630; build clean; lint at the one old warning; WebKit `phone` (keyboard, dialogs, lists) 21 passed / 11 skipped; 2 mutants killed; the kit dialog's desktop parity against `b365c10`, 16 captures: 12 identical, four at 2–4 px and a delta of 1. **One gap, said in the reply:** the full suite and the parity ran before the redundant `max-w-full` came out; after it the dialog spec re-ran green in three projects and the mutant was re-measured. CI on `cdebc8e` had just started when this was written. Throwaway database dropped, worktrees removed, no `zz-*` files, ports free, previews stopped. Drafts in the gitignored `.dev/259/`.
- **Next:** the merge on the owner's word — `gh pr merge 272 --squash` with a subject and body from the PR title and the three commit messages, after CI on `cdebc8e` is green; then the post-merge hand-off, and the branch stays on origin until the owner says. Then **#260**, scoped read-only on 2026-09-18: Settings breaks at `sm` (640) not the shell's 768 and has no phone section-list screen (`/settings` redirects to General); Home is mostly there (container queries); #269/#270/#271/#273 are one family in `ui.tsx`/`Layout.tsx`/the picker; suggested split — the pages and the siblings first, then the e2e sweep, screenshots, docs and release (the owner's call). Still Jamie's: the real-device pass, the Gunpla skill refresh. Open: #259–#260, #267 (with this PR), #268–#271, #273, #28, #238, #247, #223–#227, #230, #249.
