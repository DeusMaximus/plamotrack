# Session hand-off log

Every agent session that changes this repo appends an entry **at the top** (newest
first) before finishing. The next session may be a different agent/model with no
shared context — and possibly a small context window — so this file stays short:

- **It holds the five most recent entries.** After appending yours, if there are
  more than five, move the oldest to the top of `.agents/handoff/YYYY-MM.md` (the
  month in the entry's date), verbatim, in the same commit. Never edit or
  summarise an entry on the way out.
- **Entries are ≤ ~60 lines and carry state:** what was done, what was decided,
  what is half-finished or broken, what comes next. A *lesson* — the trap a test
  fell into, what a review round found and why it was missed — goes under its own
  heading in `.agents/lessons.md`; the entry links it in one line. *Procedure*
  that changed — how to run a check, which reviewer for what — is edited into
  `.agents/testing-and-review.md`, not narrated here.
- **The newest entry is self-sufficient about live state.** If something from an
  older entry is still true and still matters — an in-flight decision, known
  breakage, a sequencing constraint — restate it or link it. Older entries in this
  file are history and rotation will drop them; nothing live may depend on one.

Older entries live in `.agents/handoff/YYYY-MM.md`, verbatim, newest first. Do not
read an archive file whole. To find something:

```bash
grep -n '^## ' .agents/handoff/*.md      # every archived title, with its date
grep -rn '#123' .agents/handoff/         # every mention of an issue or PR number
```

then read the entry that matched.

Template:

```markdown
## YYYY-MM-DD — <agent> — <short title>
- **Done:**
- **Decisions:**
- **State:** (tests? migrations? anything half-finished?)
- **Next:**
```

---

## 2026-09-18 — Claude Code (Fable 5.1) — **PR #272: Codex round 2 was GO with three non-blocking P3s — the cycle is over by the #266 rule; the two that were round 1's own regressions fixed at `cdebc8e`, the third filed as #273; the merge is the owner's word**

- **Done:** (3, P3) the landing check after a date input (`Modal.tsx`, round 1's fix) removed itself on `setTimeout(…, 0)` alone; under a burst of undelayed native Tabs several were pending at once, each with its own next stop, and they bounced focus between two fields until the page stalled (12/12 for Codex; at `b365c10` my test never returns). The listener now removes itself **before** it sends focus anywhere; the timer only clears one whose Tab stayed inside the field. The invariant is in the code and the test: *a pending correction is consumed by its own departure and governs no later focus change.* (4, P3) *Withdraw…* is 252 px under a 40 px font in a 178 px row — round 1 had removed its word-breaking as untested; back as `stack-2:[overflow-wrap:anywhere]` on the button. (5, P3) a selected catalog item's long unbroken name pushes *Change* to 525 px on a 320 px sheet, identical at `ac00aad` — **filed as #273** (M6.6, with #269–#271). Tests: *a burst of Tabs through a date input settles* (Chromium, three projects); the font-size test **isolates the sheet** (`isolateSheet`: `#root` out of layout while measuring, the screen's width asserted). Docs: design §13.7, one lesson. PR body corrected as Codex asked (the `[274–476]` red with the current seed; the date test's engines; the stale untested row).
- **Found:** why round 1's test missed finding 4 — at 32/40 px the page *behind* the sheet is wider than a phone (#269, #270), a wide page widens the layout viewport (in a mouse context too: `innerWidth` read 381), and the sheet was measured against that. Codex's short-kit seed did not help here (the scale is derived from the grade; the card's chips alone overflow). Codex's candidate also had `max-width: 100%`; with either class alone the button's *box* fits, both single mutants survived, and the cap was removed as redundant. What the checks still do not see: text ink outside a control's box.
- **Decisions:** the owner's (2026-09-18, remote): fix 3 and 4 here rather than file them — they were regressions of this PR's own commit — file 5, no round 3, and **the merge waits for an explicit word**. Mine: a third finding in the trap's segmented-field branch means restructuring it, not patching (said in design §13.7 and the lesson). **Open:** the merge (`Closes #259, closes #267`); the iOS on-screen keyboard on a device (call 8; neither Codex nor this agent can raise one).
- **State:** `feat/259-dialog-sheets` = **`cdebc8e`** (three commits on `main`), pushed; PR #272's body rewritten (round-2 marks, mutants, parity, *r2* rows); the reply posted (issuecomment-5728706950); **#273** filed. Evidence: finding 3 red at `b365c10` as the stall (a 30 s timeout, not a named assertion — said in the body), finding 4 red on "Withdraw… [71–323] of 320" / "outside its box [40–280]" / "323 in 320"; full e2e from a database migrated from empty, `--workers=1`, **156 passed, 37 skipped**, 0 rows left; unit 630; build clean; lint at the one old warning; WebKit `phone` (keyboard, dialogs, lists) 21 passed / 11 skipped; 2 mutants killed; the kit dialog's desktop parity against `b365c10`, 16 captures: 12 identical, four at 2–4 px and a delta of 1. **One gap, said in the reply:** the full suite and the parity ran before the redundant `max-w-full` came out; after it the dialog spec re-ran green in three projects and the mutant was re-measured. CI on `cdebc8e` had just started when this was written. Throwaway database dropped, worktrees removed, no `zz-*` files, ports free, previews stopped. Drafts in the gitignored `.dev/259/`.
- **Next:** the merge on the owner's word — `gh pr merge 272 --squash` with a subject and body from the PR title and the three commit messages, after CI on `cdebc8e` is green; then the post-merge hand-off, and the branch stays on origin until the owner says. Then **#260**, scoped read-only on 2026-09-18: Settings breaks at `sm` (640) not the shell's 768 and has no phone section-list screen (`/settings` redirects to General); Home is mostly there (container queries); #269/#270/#271/#273 are one family in `ui.tsx`/`Layout.tsx`/the picker; suggested split — the pages and the siblings first, then the e2e sweep, screenshots, docs and release (the owner's call). Still Jamie's: the real-device pass, the Gunpla skill refresh. Open: #259–#260, #267 (with this PR), #268–#271, #273, #28, #238, #247, #223–#227, #230, #249.

## 2026-09-18 — Claude Code (Fable 5.1) — **PR #272: Codex round 1 was GO with two non-blocking P3s; both reproduced, fixed and answered at `b365c10`** — the applied-upgrade row under a large font, and WebKit passing *Add line* over after a date input; **a round 2 on the delta is Codex's** (the owner's call); the iOS keyboard still waits for a device

- **Done:** (1, P3) the kit form's applied-upgrade row (name, date, *Withdraw…*) was 16 px past a 390 px sheet at a 32 px browser font and 86 px past a 320 px one — the state the PR's record had under "explicitly untested". `KitFormModal.tsx`: the row stays `main`'s where it fits; the name is `min-w-0 break-words` (a word no line can hold breaks), `stack-3:flex-wrap` gives Withdraw the next line, the question's sentence-long buttons are `stack-2:[overflow-wrap:anywhere]`. (2, P3) WebKit left the order form's arrival date for the first line's type select, *Add line* skipped, focus inside the whole time. `Modal.tsx`: the date keeps its own Tab; for the length of that key's task a `focusin` anywhere but the logical next stop is sent on to it. Tests: the dialog spec's kit carries an applied upgrade with a **42-character token**; the fit and font tests open the question, ask whether every control is inside its **own card** (`uncarded`) and whether the name reads and has its room (`expectUpgradeNameReads`); the date-parts test asserts the stop **by name**, under WebKit too, on the edit dialog and on the review's path. Docs: design §13.7, one lesson.
- **Found:** Codex's remedy direction (`flex-wrap` on the row) would have sent Withdraw to the next line on the **desktop** for any long name — a short-named parity capture could not see it. With a short name, then a 29-character token, the row's remedies covered for each other and three mutants survived: "inside the screen" is not "inside its card". Word-breaking on the row's own button was removed rather than kept untested (no tested point needs it in English). A `pkill` of a Playwright run leaves its uvicorn alive, holding the throwaway database — the next "from empty" run then is not; check `lsof :8000` and the row counts *before* the run.
- **Decisions:** Codex's corrections accepted — the negative control at `ac00aad` is **7 red / 2 green** (I tightened the record test after counting), M12 fails on either twin. The owner's (2026-09-18, remote): fix rather than file; commit, push, reply; a round 2 on the delta; then scope #260. **Open:** Codex's round 2; the merge (`Closes #259, closes #267`); the iOS keyboard on a device (call 8 — neither Codex nor this agent can raise it).
- **State:** `feat/259-dialog-sheets` = **`b365c10`** (two commits on `main`), pushed; PR #272's body rewritten (round-1 marks, the round's mutants and parity, *r1* rows); the reply posted (issuecomment-5728124204). At `b365c10`: both findings red at `ac00aad` on the named assertions; full e2e from a database migrated from empty, `--workers=1`, **153 passed, 37 skipped**, 0 rows left; unit 630; build clean; lint at the one old warning; WebKit `phone` (keyboard, dialogs, lists) 21 passed / 10 skipped; **5 round-1 mutants, 5 killed**; the row's desktop parity against `ac00aad`, question closed and open, 1440/1280/1024/820 × two themes: 14 of 16 identical, two at 2 px and a delta of 1. Not checked by anyone: the on-screen keyboard on a device, Safari itself, a screen reader, RTL, page zoom, the packaged stack. Throwaway database dropped, worktrees removed, no `zz-*` files, ports free, previews stopped. Drafts in the gitignored `.dev/259/` (`review-reply-1.md`, `commit-msg-r1.txt`, `review-brief-2.md`).
- **Next:** Codex's round 2 on the delta — the owner passes one line pointing at `.dev/259/review-brief-2.md`; answer it per `.agents/testing-and-review.md`; by the rule agreed on #266, a GO with nothing above a P3 remedy ends the cycle and the merge is the owner's word. Then **#260** (Home, Settings, sign-in on a phone, the full phone e2e and the release — with #269, #270, #271); scoping it has started, read-only. Still Jamie's: the real-device pass (the keyboard), the Gunpla skill refresh at upgrade time. Open: #259–#260, #267 (with this PR), #268–#271, #28, #238, #247, #223–#227, #230, #249.

## 2026-09-18 — Claude Code (Fable 5.1) — **#259 built, PR #272 open against `main`** (M6.6 3/4: every dialog a full-screen sheet below 768 px with a fixed action bar, order lines that stack, rows that fold by their box; the trap holds in WebKit, #267); **two WebKit-only defects older than the milestone found and fixed** (the picker's click, the late focus drop); **Codex briefed**

- **Done:** `Modal` draws three frames from one tree (`max-md:`): the desktop's panel, the phone's sheet (head and bar fixed by a flex column, the body the one scroller, `@container`) and the bottom sheet; a form *describes* its two actions and its Delete (`DialogAction`), the frame draws them — the desktop's row as it was, the phone's bar 1:2 at 48 px above the inset; Delete drawn twice, CSS-hidden per shell, keyed `dialog-delete` (`focusByKey` reads the document now). `OrderFormModal`: the head's eight fields one six-column grid with spans per shell; a line a card (type + remove, quantity and price under labels with the code inside the field, name, three across); `stack-3:`/`stack-2:` container variants (index.css, 6.25rem a field + gaps) on every form's rows and on the line card. The trap: hand-driven across what Safari skips, native between fields, a date input keeps its parts (#267). The recapture acts on a control disabled *now* and on a `focusout` to nowhere, key first. The picker's list keeps the input's focus through a mousedown. `useFollowVisualViewport` for the iOS keyboard (CSSOM). `inputmode`, 44 px checkbox rows, picker rows, the remove control. Tests: `e2e/dialogs.spec.ts` (new, 5 tests × 3 projects), `dialog-keyboard.spec.ts` in `phone`/`tablet` + a date-parts test, `shell.test.ts` holds the two lines. Docs: design §13.7 "Built — #259", `AGENTS.md`, README row, `testing-and-review.md` (WebKit runs), two lessons.
- **Found:** (1) Safari does not focus a button on click → #104's `relatedTarget` rule unmounted the picker's list before the click: **no catalog item could be picked by tap on an iPhone**, since #104 (fixed; `lessons.md`). (2) WebKit drops focus from a just-disabled button *after* the observer looks (fixed; lesson). (3) My first `focusout` fallback pre-empted the Delete twin on a turn (the rotation test caught it in the same run). (4) A one-axis containment check let a mutant survive (M5: the code under the field); a click had scrolled the primary into view on `main` so "without a scroll" passed there (both tightened). (5) `stack-2:` beats `max-md:grid-cols-2` only because Tailwind emits the container rule later — read off `dist/`.
- **Decisions:** mine, calls 1–12 in the PR body; the two the owner may want to look at: **call 4** — the line keeps the desktop's DOM order (type/remove, quantity/price, then the name; the issue drew the name first) so one tree serves both shells; **call 7** — the two older WebKit defects fixed here, not filed. The owner's: build it (2026-09-18). **Open:** the Codex review; the merge (`Closes #259, closes #267`); the iOS keyboard on a device.
- **State:** `feat/259-dialog-sheets` = **`ac00aad`**, pushed; **PR #272`** open against `main`, body in the review shape; `main` = `f6e81f9`. 17 files + `e2e/dialogs.spec.ts`; no backend change. At `ac00aad`: full e2e from empty, `--workers=1`, **153 passed, 37 skipped**, 0 rows left; unit 630; build clean; lint at the one old warning; negative control on `main`'s `src/` 6 red / 3 green (the panel-frame tests in `app`/`tablet`, unchanged by design; the record test's first form); **18 mutants, 18 killed (two after their tests were rewritten)**; WebKit local: keyboard 8/8, dialogs 4/4, lists+shell 39/0; parity 136 captures a side at 1440/1280/1024/820 × two themes against `main`: 103 identical, the rest inside the noise floor but the Apply dialog's sub-pixel button edge (call 10; the line row's 4 px restored after the first pass showed every order dialog shifted). Not checked by anyone: the on-screen keyboard on a device (iOS/Android), Safari itself, a screen reader, RTL, page zoom, the packaged stack (no ingress/CSP change). Throwaway database dropped, worktree removed, `zz-*` files removed, ports free; the browser pane's previews stopped. Drafts in the gitignored `.dev/259/`.
- **Next:** the **Codex review of PR #272** — the old way, through the PR thread (a browser leg; the plugin cannot launch Chromium): the brief is `.dev/259/review-brief.md`, the owner pastes one line into the Codex *app*. Answer it per `.agents/testing-and-review.md` → "Responding to a review"; merge on the owner's word. Then **#260** (Home, Settings, sign-in on a phone, the full phone e2e and the release — with #269, #270, #271). Still Jamie's: a real-device pass (the keyboard!), the Gunpla skill refresh at upgrade time. Open: #259–#260, #267 (with this PR), #268–#271, #28, #238, #247, #223–#227, #230, #249.

## 2026-09-18 — Claude Code (Fable 5.1) — **#258 MERGED as `b9e6acb`** (PR #266, squash: M6.6 2/4 — card rows, one filter sheet, tables that fold to their box, focus by record, what a line cannot know giving way by its value); **four Codex rounds** (NO-GO ×2, GO ×2); the last GO's two non-blocking P3s filed as **#270** (a card's facts under a large font) and **#271** (the stock count's second arrangement); next is **#259**

- **Done:** Codex's round 4 at `0b59baa` — GO, finding 7 closed, two P3s opened — answered per the rule agreed with the owner before the round (a GO with nothing above a P3 remedy ends the cycle): both reproduced at `0b59baa` (an ordinary MGEX 1/100 pre-ordered kit's grade at 333 and scale at 380 past a 287 px card at 32 px, the document 380 px on a 320 px screen; `1,234` puts *Add* at 257–301 past 279 at 40 px) and **filed**, not fixed; the reply posted; the body's round-4 marks (the 22 → 23 correction, the ≥ 128 px claim scoped to 32/40, the font-arrival prose corrected); one test-only commit `5a4442f` (the font-arrival test annotates a fallback with the web font's metrics instead of failing); then the squash merge on the owner's word, `Closes #258`.
- **Found:** the round-3 "class" was the *controls'* class — the facts' line (`CardMeta`, no wrap; `Chip` `nowrap`; grade and scale `shrink-0`) is the content's, and the font-size test never asserted the document's width; three digits fit the stepper at 40 px (the stated bound was conservative), `12` does not at 56 px; the ≥ 128 px room holds at 32/40 only (118/92 px for a kit title at 48/56); F7e/F7i/F7k are distinguished at 48/56 px — reasons for those points, filed with #270; the pencil at 12 px is 44 × 33 (a width floor only).
- **Decisions:** the owner's — a round 4 rather than a merge on the round-3 GO, and the merge on this GO without a round 5 (2026-09-18). Mine — 8 and 9 filed rather than fixed (they reach `CardMeta`/`Chip`, every card and table; this branch had its last reviewed head), the test-only commit included in the merge and said so in the reply. **Open:** #270, #271 (with #269, all the cards' and shell's rem sizes under a large font — #260's territory), #267 (with #259), #268.
- **State:** `main` = **`b9e6acb`** (+ this entry); `feat/258-list-pages-phone` at `5a4442f` still on origin (deleting merged branches is on the owner's word, as before). Evidence at `0b59baa` in the PR body's record and round 4's comment (full e2e 127/31/0 both sides; unit 628; WebKit 24/1; 16 mutants 12 killed; CI green); `5a4442f`: the font-arrival test green in Chromium `app`/`tablet` and WebKit `tablet`, no source change. Throwaway database dropped, no worktree, no `zz-*` files, ports free. Drafts in the gitignored `.dev/258/`.
- **Next:** **#259** (dialogs as sheets; #267 belongs with it), then #260 (Home, Settings, sign-in, the full phone e2e and the release — with #269, #270, #271). Still Jamie's: docs `tokens.png` re-shoot, a real-device pass, the Gunpla skill refresh at upgrade time. Open: #259, #260, #267–#269, #270, #271, #28, #238, #247, #223–#227, #230, #249.

## 2026-09-18 — Claude Code (Fable 5.1) — **PR #266: Codex round 3 was GO with a non-blocking P3; reproduced, fixed as the class and answered at `0b59baa`** — a card's controls give way to a finger's floor and the name keeps its share (the round-2 decline was a miscount); the three "equivalent" ruler mutants the reviewer killed are seeds in the suite now; **the merge is the owner's call** (round 4, or not)

- **Done:** (7, P3) the stock stepper at 320 px under a 32 px browser font was 46 px past its card — declined in round 2 as "three 44 px targets, 272 px in a 204 px row"; there are two targets and a count. Fixed as the class in `InventoryPage.tsx`, `ui.tsx` (`IconButton`, `CardRow`) and `OrdersPage.tsx` (the card): a control's size in rem, its **floor in px** (`min-w-[44px]`; the toggle's 36; no `shrink-0` on anything standing in a card's row — the pencil's `<span>` wrapper went, its `stopPropagation` moved into the handler), the stepper `max-w-full` in a `min-w-0` wrapper with px gaps and a count that never shrinks; and the identifying column **`flex-[1_1_8rem]`** instead of `flex-1`, so each item yields in proportion and the controls reach their floors first. Same pixels at 16 px (the floor is the size). Codex's three probes that killed W7/W8/W9 are in `lists.spec.ts`: `NARROW_TRACKING` (twenty `1`s), `UNBOUNDED_NUMBER` (a hundred `M`s; its sizer asserted wider than the box), `CROSSING_TRACKING` (thirteen `8`s) with a new test that holds the `.woff2` requests (`page.route`) until the fallback has been measured. The coverage record's provenance split as Codex asked.
- **Found:** the second point on the font-size axis (40 px) found what a fix for the stepper alone would have left — the order card's toggle and pencil (90 and 110 px) leaving the retailer 38 px and the total out of its column; and the count shrinking under its digits (`1,234` cut) once the stepper could give way. A flex item with basis 0 gets what its `shrink-0` neighbours leave; a wrapper `<span>` round a control has the control's width as its minimum. The bound that remains: a count wider than the row less two fingers — four digits at 32 px, two at 40, on a 320 px phone (probed, in the record). The pencil's, the toggle's and the count's px floors are not reached at 32 or 40 px, so their mutants (F7e, F7i, F7k) survive for the tested points, and the tuples say so.
- **Decisions:** mine, calls 16 (revised: fixed, not declined), 17 and 18 in the PR body. Codex's round-3 verdicts on 11 and 15 accepted. The owner's: commit, push, post, hand-off, and **a round 4 on the delta** rather than a merge on the round-3 GO (2026-09-18) — because every round so far found something in the fix from the round before, and this one reached a shared primitive. **Open:** Codex's round 4; the merge (`Closes #258`).
- **State:** `feat/258-list-pages-phone` = **`0b59baa`**, pushed; PR #266's body rewritten (round-3 marks, calls 16–18, the record); the round-3 tuples appended to the tuples comment; the round-3 reply posted. 8 files this round (3 source, the spec, 4 docs). At `0b59baa`: full e2e from empty, `--workers=1`, **127 passed, 31 skipped**, 0 rows left, settings restored; unit 628; build clean; lint at the one old warning; `lists.spec.ts` 17 tests × 3 projects; negative control on `0539eba`'s `src/`: 25 / 1 / 26, the one red the font-size test on 22 named assertions, no timeout; **16 mutants, 12 caught, 4 surviving** (F7e/F7i/F7k for the tested points, W6 observational; W7/W8/W9 now killed); `app`, `tablet`, `phone` ×3 in fresh processes green; **WebKit** **24 passed, 1 failed, 16 skipped** across `phone`, `tablet` and `settings` (lists.spec.ts and lists.settings.spec.ts) — the one failure the sheet's Shift+Tab escape, #267, unchanged; the font-arrival test passes there with the same crossing (8.05em → 8.43em), once its navigation stopped waiting for `load` (WebKit's `load` waits for the held font) and its precondition read the faces' own `status` (WebKit's `fonts.check()` answers true for a face still loading); the font-size test is Chromium's flag and skips; ordinary rows against `0539eba`: 192 captures a side at six sizes, every difference inside the head-against-itself noise but one 23-pixel antialiasing delta in Kits' search row at 390 px (crops indistinguishable). Not checked by anyone: a real device, Safari, a screen reader, Firefox, RTL, page zoom, font sizes other than 16/32/40, the packaged stack. Throwaway database dropped, worktrees removed, no `zz-*` files, ports free. Drafts in the gitignored `.dev/258/` (`review-reply-3.md`, `pr-body-r3.py`, `commit-msg-r3.txt`, `review-brief-4.md`).
- **Next:** Codex's round 4 on the delta — the owner pastes one line pointing at `.dev/258/review-brief-4.md` into the Codex *app*; answer it per `.agents/testing-and-review.md` → "Responding to a review"; if it is a GO with nothing above a P3 remedy, merge on the owner's word without a round 5 (`Closes #258`). After the merge: **#259** (dialogs as sheets; #267 belongs with it), #260 (with #269 — the shell under a large font). Still Jamie's: docs `tokens.png` re-shoot, a real-device pass. Open: #258–#260, #267–#269, #28, #238, #247, #223–#227, #230, #249.
