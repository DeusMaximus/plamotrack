# Session hand-off log

Every agent session that changes this repo appends an entry **at the top** (newest
first) before finishing. The next session may be a different agent/model with no
shared context — and possibly a small context window — so this file stays short:

- **It holds the five most recent entries.** After appending yours, if there are
  more than five, move the oldest to the top of `.agents/handoff/YYYY-MM.md` (the
  month in the entry's date), verbatim, in the same commit. Never edit or
  summarise an entry on the way out.
- **Entries are ≤ ~60 lines and carry state:** what was done, what was decided,
  what is half-finished or broken, what comes next. A *lesson* — the trap a test
  fell into, what a review round found and why it was missed — goes under its own
  heading in `.agents/lessons.md`; the entry links it in one line. *Procedure*
  that changed — how to run a check, which reviewer for what — is edited into
  `.agents/testing-and-review.md`, not narrated here.
- **The newest entry is self-sufficient about live state.** If something from an
  older entry is still true and still matters — an in-flight decision, known
  breakage, a sequencing constraint — restate it or link it. Older entries in this
  file are history and rotation will drop them; nothing live may depend on one.

Older entries live in `.agents/handoff/YYYY-MM.md`, verbatim, newest first. Do not
read an archive file whole. To find something:

```bash
grep -n '^## ' .agents/handoff/*.md      # every archived title, with its date
grep -rn '#123' .agents/handoff/         # every mention of an issue or PR number
```

then read the entry that matched.

Template:

```markdown
## YYYY-MM-DD — <agent> — <short title>
- **Done:**
- **Decisions:**
- **State:** (tests? migrations? anything half-finished?)
- **Next:**
```

---

## 2026-09-22 — Codex (GPT-6) — #278 release packaging implementation on `codex/278-release-packaging`; commit/push authorized

- **Done:** read the live issue, repository instructions and current handoff; fetched origin and verified clean main at `f82ad7a` before branching. Added pull-only Compose plus explicit source-build override; API/migrate share an image, source tags and `pull_policy: build` exclude published-image substitution. Added revision/version image labels, deterministic skill ZIP and release bundle generator/checksums, manually dispatched candidate and promotion workflows, native amd64/arm64 candidate CI, and running-image/version checks. Promotion requires an existing tag at the successful candidate's exact SHA, copies digests without rebuilding, refuses conflicting version tags, and creates only a draft release. Source deployment-gate calls now require `--source-build`; release bundles use `--no-build`. README, configuration examples, design and maintainer procedures updated.
- **Decisions:** no moving release-channel tags; explicit versions and literal digests in downloadable Compose, including Postgres. The ZIP remains `plamotrack-gunpla.zip`, versioned by its release URL and manifest. Candidate reruns are not promotable: dispatch a fresh run so partial reruns cannot mix old gates and new artifacts. The normal PR check remains named `Integration`. First GHCR use needs owner-confirmed public package visibility; native jobs must pull anonymously. No release version chosen or bumped. Historical LXC build cause remains unknown.
- **Validation:** focused backend suites **48 passed**, Ruff check/format and actionlint passed. Both API/web architectures built; independent local stacks migrated fresh databases, served the app, and passed the packaged ingress matrix (**190 ARM64 / 190 AMD64 positive rows, zero failures**). ARM64 ran natively; AMD64 ran under OrbStack emulation. Both passed authenticated REST/current modern and forced-legacy MCP/version checks and non-vacuous credential-log scans. Image identity checks passed; wrong-source revision was refused. **4/4 single-site mutants killed**, restored byte-for-byte (pull policy, candidate SHA, rerun attempt, version overwrite). Evidence: `.dev/278/implementation-evidence.md`, build/start/ingress logs, `pytest-final.log`, `mutations.json` (gitignored).
- **State:** the owner approved the implementation and explicitly requested commit/push; this commit records the implementation and handoff on `codex/278-release-packaging`. No PR or GitHub comment, no tag/release/registry publication. #278 remains open. No claim of native GitHub runs, anonymous GHCR pulls or live promotion yet; full backend/e2e and remote TLS/OIDC gate not rerun locally. Disposable test stacks/volumes removed; the pre-existing development DB remains running. Local build images/evidence retained. Testhost, production and docs-site repo untouched. Current published release remains v0.5.0-alpha without the new assets.
- **Next:** verify the authorized branch push, then open the PR when requested. This changes the shared release/distribution boundary and warrants independent review before landing; none commissioned yet. After review, authorize the first candidate publication, verify public GHCR pulls and both native hosted gates, run the artifact deployment/client gate, then tag/promote/publish as separately authorized in `.agents/releases.md`. #278 acceptance is not complete until that publication evidence exists; #279/#280 remain separate investigations, and #285 owns existing-install upgrades. The handoff keeps five entries and rotates the oldest verbatim in this commit.

## 2026-09-20 — Codex (GPT-6) — PR #288 MERGED as `9093ea8`; M6.7 roadmap recorded, sandbox authority clarified; all ten implementation issues remain open

- **Done:** on the owner's request to apply the Greptile clarification and merge, added the explicit sandbox-principal/default-deny-registry requirement to design §11.1 and issue #286. Pushed `d0832fb`, updated the PR coverage record, posted the attributed response (issuecomment-5748634132), and resolved Greptile's planning thread. Waited for the final commit's **Frontend, Integration and Backend CI to pass**; Backend took 18m46s, Integration 9m45s. Squash-merged [PR #288](https://github.com/DeusMaximus/plamotrack/pull/288) with `--match-head-commit d0832fb…` as **`9093ea8`**. Fetched and fast-forwarded local main, verified the merge tree equals the gated head, and checked **#278–#287 all remain OPEN** in milestone 18.
- **Decisions:** the owner explicitly declined CodeRabbit's archived-wording nit; that text remains verbatim. Greptile's issue was a planning ambiguity, not a runtime vulnerability: visitors must use a restricted principal available only in the dedicated sandbox deployment, through the existing default-deny route-policy registry; no owner authority, no broadened production anonymous scopes, no production resolution of sandbox visitors. #286 now requires route/operation mapping plus denied-action and production-regression tests. Greptile did not post a new review on the clarification commit; its original comment was addressed and resolved, not described as a fresh approval. CodeRabbit's latest status was a successful skip.
- **Milestone state:** [M6.7](https://github.com/DeusMaximus/plamotrack/milestone/18) is planned, not implemented: **#278** images/assets, **#279** host investigation, **#280** Pocket ID investigation, **#281** platform template, **#282** VPS guide, **#283** first run, **#284** assistant connection, **#285** updates/backups/recovery, **#286** editable daily-reset sandbox, **#287** complete-journey docs/usability/release acceptance. Start #278–#280. M9 retains contributor guidance and the broader support matrix. M7's photo-storage #28 and M8's personal showcase remain separate.
- **State:** main contains the merged roadmap and this bookkeeping entry; `codex/m6-7-roadmap` remains locally and on origin (no deletion requested). No application implementation, deployment, hosting purchase or docs-site edit. Current install instructions still apply; provider selection and Pocket ID compatibility remain unverified. Latest recorded app release is v0.5.0-alpha; the owner's production upgrade and skill refresh were not checked. Existing defects and testhost were untouched. Evidence: `.dev/m6.7-filing/merge-gate.json`, `merged.json`, final PR/issue bodies and review reply (gitignored).
- **Validation:** design paragraph matches the updated issue body, read back byte-for-byte; diff checks passed; full final-head CI succeeded, including the packaged-stack/ingress/MCP Integration leg. No runtime tests or mutations were added for the prose clarification. Handoff rotation preserves five live entries and the archive text verbatim.
- **Next:** begin #278–#280 when implementation is requested. These investigations establish later provider-specific work; no host or Pocket ID support claim is made by the merged plan. Keep the unexplained LXC build failure's cause unestablished.

## 2026-09-20 — Codex (GPT-6) — M6.7 roadmap committed and pushed; PR #288 open against main

- **Done:** on the owner's request to commit, push and open the PR, committed the five documentation/handoff files as `3cba122` (`docs: plan M6.7 hosted deployment and public sandbox`), pushed `codex/m6-7-roadmap`, and opened [PR #288](https://github.com/DeusMaximus/plamotrack/pull/288) against `main`, assigned to M6.7 with the documentation label. Its body is attributed, includes the documentation coverage record, and says “Part of #287”; GitHub's closing-issue references were checked and are empty. This entry records the publication state in a follow-up bookkeeping commit on the same branch.
- **Decisions:** the user authorised commit/push/PR, not a merge or feature implementation. No external review commissioned: this is a roadmap/docs-only change, not a shared runtime mechanism. The milestone remains [M6.7 — Easy hosted deployment and public sandbox](https://github.com/DeusMaximus/plamotrack/milestone/18): public images; one verified platform path and one VPS path; guided claiming, optional assistant connection, updates/backups/recovery; a separate shared editable sandbox with invented data and daily reset. Host choice and Pocket ID compatibility remain investigations. Sandbox REST is restricted server-side; MCP/OAuth, credentials and administrative mutations are unavailable in the plan. Production authentication, M7's #28 and M8's personal showcase remain separate.
- **Issue map:** #278 images/assets, #279 platform investigation, #280 Pocket ID investigation, #281 platform template, #282 VPS setup, #283 first run, #284 assistant connection, #285 operations, #286 sandbox, #287 complete-journey acceptance. All ten were verified open with their expected labels in milestone 18. M9's description now retains contributor guidance and the broader support matrix.
- **State:** PR #288 is open, ready for review; branch `codex/m6-7-roadmap` is based on `main` at `553b5af`. CI started on the initial documentation commit; no green-CI claim is made here, and the follow-up handoff commit receives its own checks. No application changes, deployments, hosting purchases or docs-site edits. Current operator instructions still describe released behaviour. Latest recorded app release is v0.5.0-alpha; the owner's production upgrade and skill refresh were not checked. Testhost and the existing open defects were untouched. PR body and issue-filing evidence are in gitignored `.dev/m6.7-filing/`.
- **Validation:** documentation diff reviewed; `git diff --check` passed; issue links/labels/milestone verified; handoff keeps five live entries and rotates the oldest verbatim. Runtime tests and mutation tests do not apply to this docs-only change. The PR body explicitly leaves provider/sandbox runtime verification to the implementation issues.
- **Next:** review PR #288 and its final-head checks; merge only on the owner's request. Start #278–#280 when implementation is requested, and let their evidence determine later provider-specific work. Keep the existing LXC build failure's cause unestablished.

## 2026-09-20 — Codex (GPT-6) — M6.7 filed: hosted deployment and editable public sandbox; roadmap docs updated locally, uncommitted

- **Done:** the owner approved filing the reviewed plan and updating docs. Created [M6.7 — Easy hosted deployment and public sandbox](https://github.com/DeusMaximus/plamotrack/milestone/18), ten issues **#278–#287** with existing labels and model attribution; replaced every draft dependency ID with real issue references and verified all ten remote bodies byte-for-byte. Updated M9's description to retain contributor guidance and the broader compatibility/support matrix. Updated `README.md`, `AGENTS.md` and `docs/design.md` (§8, §11 and new §11.1); planned work is explicitly unshipped, and the current install commands still apply.
- **Decisions:** the owner's target is a separate internet-accessible instance paid for by its owner, with one recommended platform path and one portable VPS path. Public images first; **#279** selects the host through a working deployment, **#280** verifies Pocket ID for both browser login and assistant OAuth/refresh/recovery before recommending it. **#286** is a shared editable sandbox with invented data, real collection workflows and normal integrity guards, daily reset, visible temporary/shared-data notice and bounded public writes; account/settings/credential mutations and MCP/OAuth are unavailable. Browser collection REST endpoints remain necessary and callable directly, with server-enforced limits. Separate sandbox artifact/data; no general production authentication-off setting. M7's #28 and M8's personal showcase remain separate.
- **Issue map:** **#278** release images/assets; **#279** hosting investigation; **#280** Pocket ID investigation; **#281** platform template; **#282** VPS setup; **#283** first run; **#284** assistant connection; **#285** updates/backups/recovery; **#286** sandbox; **#287** docs/usability/release acceptance. Start #278–#280; the subsequent provider-specific details follow their results. #278 also packages the compatible skill ZIP.
- **State:** branch `codex/m6-7-roadmap` from clean, remote-matched main `553b5af`; documentation and this handoff/rotation are **uncommitted**, nothing pushed, no PR. No application implementation, deployments or hosting purchases. Filing evidence and final body files in gitignored `.dev/m6.7-filing/`; the reviewed source draft is `/Users/tlgja/Downloads/M6.7-milestone-and-issues-draft.md` (its “not filed” status is now historical). The docs-site repo was not changed: its operator guides continue to describe released behaviour. Latest recorded app release remains v0.5.0-alpha; the owner's production upgrade and skill refresh were not verified this turn. Existing open bugs, including #268/#238/#247, were not reassigned. Testhost was untouched.
- **Validation:** remote issue bodies, milestone assignment/labels, dependency references and both milestone descriptions checked; documentation diff/links and verbatim five-entry handoff rotation checked. No runtime tests: documentation-only changes.
- **Next:** review/commit/push these documentation edits only on the owner's request. Implementation starts with #278–#280 when requested. Hosting provider and Pocket ID support are not settled. Preserve the known unexplained LXC failure as an observation, not a new causal claim.

## 2026-09-20 — Claude Code (Fable 5.1) — **v0.5.0-alpha RELEASED** — M6.6, the phone and tablet layout: release PR #277 merged as `ba770e3`, tag and prerelease published, plamotrack-docs#6 merged (`e37c9ba`, live on docs.gunp.la), **#260 closed, the M6.6 milestone closed**

- **Done:** the owner's word ("yep, let's go for it", to all five steps asked separately): `gh pr merge 277 --squash --match-head-commit 41a9fa4…` → **`ba770e3`** (`main`'s code is `41a9fa4`'s, checked; #260 still open after the merge, checked); annotated tag `v0.5.0-alpha — phone and tablet layout` on `ba770e3`, pushed; `gh release create v0.5.0-alpha --prerelease --verify-tag` with the gated notes (they lead with "nothing to migrate" and the one desktop change; the gate block is as observed, the workstation's public address redacted); plamotrack-docs#6 squash-merged after the release so its changelog link resolves — `docs.gunp.la/using/phone-and-tablet` answers 200 and the upgrading page carries v0.5.0-alpha; #260 closed with a note against its *Done when* list (which says plainly that no real iPhone was used); milestone 17 closed at 0 open.
- **Decisions:** the owner's: version 0.5.0; the real-client MCP acceptance run skipped (nothing under `/mcp` changed since 0.4.1); the notes as drafted, internal addresses kept as v0.4.1's were. Mine: the merged branches were **not** deleted — the offer was conditional and the answer did not say; `feat/260-phone-pages`, `feat/260-phone-docs` and `release/0.5.0` are still on origin (as `feat/259-dialog-sheets` and earlier ones are), on the owner's word.
- **State:** `main` = **`ba770e3`** + this entry, tagged `v0.5.0-alpha`; the tree is parked on `main`, clean. Gate evidence is in the release notes and `.dev/release-0.5.0/` (gitignored: `gate-results.md`, `gate-run.log`). Testhost is as the gate left it (mode R, the OIDC fixture, the gate's own owner, the 0.5.0 release tree). **The real instance (the LXC) is still on v0.4.1-alpha** — upgrading it is the owner's (dump + `.env` first; no migration, `git pull && docker compose up -d --build --wait`). Not checked by anyone for 0.5.0: a real iPhone, a screen reader, RTL, page zoom, Firefox, text above 40 px. No servers running, no throwaway databases, no `zz-*` files.
- **Next:** Jamie's: the LXC upgrade, then the Gunpla skill refresh to match it. Open and unscheduled: **#268** (the stock stepper drops focus on every press — the last open M6.6-era focus defect), #238, #247, #249, #223–#227, #230; the open offer of the better *desktop* picker result row as an owner's exception (two lines of classes, scoped out in #274 round 2). **M7 (photos) waits on #28** — the storage-backend decision (design §9.2) — and the phone is now the camera it was sequenced for.

